This `reference` directory contains deprecated or otherwise reference scripts
which may have future benefits.

- setup.sh : Original bash shell script to install `hi_score`.
  it is retained for reference.

